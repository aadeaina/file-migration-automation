[pytest]
DJANGO_SETTINGS_MODULE = config.settings
python_files = tests.py test_*.py *_tests.py
markers =
    temporal_integration: needs a local Temporal dev server on localhost:7233 (excluded by default)
addopts = -m "not temporal_integration"
